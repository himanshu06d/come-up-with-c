#include<stdio.h>
int main()
{
	int h,b,a;
		printf("Enter the height of triangle = ");
		scanf("%d",&h);
		printf("Enter the base length = ");
		scanf("%d",&b);
		a=h*b/2;
		printf("The area of triangle is %d",a/n);
		return 0;
}
